import { useCallback, useEffect, useState } from 'react'

export default function useScroll(threshold: number) {
  // check session
  // check db if bio exist
  // if session AND bio does not exit
  // redeirect to sign up
}
